spring:
  application:
    name: queueify-campaign-service

  datasource:
    url: jdbc:mysql://localhost:3306/queueify
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

  mail:
    host: smtp.gmail.com
    port: 587
    username: teamqueueify@gmail.com
    password: bpqjqvltunloehzc
    default-encoding: UTF-8
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          # Socket Timeouts to Prevent Thread Blockage
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000
          # Localhost override to prevent EHLO handshake delays
          localhost: localhost

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info